const flex = {
  display: 'flex',
  alignItems: 'center',
};
const flexJust = {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
};
const flexSpace = {
  display: 'flex',
  justifyContent: 'space-between',
};

const display = {
  flex,
  flexJust,
  flexSpace
};

export default display;
